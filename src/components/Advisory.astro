---
interface Props { title?: string; }
const { title } = Astro.props;
---
<aside class="advisory" role="note">
  <span class="advisory__badge" aria-hidden="true">!</span>
  <p class="advisory__body">
    {title && <strong>{title}. </strong>}<slot />
  </p>
</aside>
